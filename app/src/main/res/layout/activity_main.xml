<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/baf_background">

    <!--
        layout_height="0dp" + layout_weight="1":
        Das Fragment bekommt den gesamten Platz ÜBER der BottomNav.
        → adjustResize funktioniert jetzt korrekt:
          Wenn Tastatur erscheint, schrumpft dieses Fragment,
          das Eingabefeld bleibt sichtbar ÜBER der Tastatur.
        → Wenn BottomNav GONE ist (Chat, Profil etc.),
          füllt das Fragment den gesamten Screen.
    -->
    <fragment
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <!--
        BottomNav sitzt jetzt UNTERHALB des Fragments (kein Overlay mehr).
        → Kein paddingBottom in Fragmenten nötig für die NavBar selbst.
        → Letzter Eintrag in Listen ist immer vollständig sichtbar.
    -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/baf_black"
        app:itemIconTint="@color/nav_item_colors"
        app:itemTextColor="@color/nav_item_colors"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
