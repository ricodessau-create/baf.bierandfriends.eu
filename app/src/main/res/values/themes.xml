<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.BAF" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Farben -->
        <item name="colorPrimary">@color/baf_gold</item>
        <item name="colorOnPrimary">@color/baf_black</item>
        <item name="colorSurface">@color/baf_dark_gray</item>
        <item name="android:colorBackground">@color/baf_black</item>

        <!-- Schrift -->
        <item name="android:fontFamily">@font/montserrat</item>

        <!-- Buttons -->
        <item name="materialButtonStyle">@style/BAF.Button</item>

        <!-- Cards -->
        <item name="materialCardViewStyle">@style/BAF.Card</item>

        <!-- Navigation -->
        <item name="bottomNavigationStyle">@style/BAF.BottomNav</item>
    </style>

</resources>
