name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Schritt 1: Firebase Datei erstellen (verhindert den Crash)
      - name: Create google-services.json
        run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > app/google-services.json

      # Schritt 2: SHA-1 für Firebase ausgeben
      - name: Generate Signing Report (SHA-1)
        run: ./gradlew signingReport

      # Schritt 3: Die App bauen
      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      # Schritt 4: Die fertige APK hochladen
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: BierAndFriends-Debug
          path: app/build/outputs/apk/debug/*.apk
          
